<Page
    x:Class="UwpNetworkingEssentials.ChatSample.ServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="SkyBlue" Padding="12">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}"
                       Foreground="Black">
                Server on port <Run Text="{x:Bind ViewModel.Server.Port, Mode=OneWay}"/>,
                <Run Text="{x:Bind ViewModel.ClientCount, Mode=OneWay}"/> client(s)
            </TextBlock>
        </Border>

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Messages}"/>

        <TextBox Grid.Row="2"
                 x:Name="messageTextBox"
                 PlaceholderText="Your message"
                 KeyDown="TextBox_KeyDown"/>
    </Grid>
</Page>
